# Policies

`review_escalation_policy.yml` is a synthetic public-preview policy. It defines proposer roles, reviewer roles, reviewable risks, evidence requirements, and public reason codes.

The policy is intentionally compact so reviewers can inspect the control surface without needing private Maxwell implementation details.
