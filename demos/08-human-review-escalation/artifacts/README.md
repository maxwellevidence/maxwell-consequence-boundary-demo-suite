# Artifacts

Generated run artifacts are written to `artifacts/runs/` when you run `make demo`.

That generated folder is intentionally excluded from public packaging. Sample outputs live under `artifacts/sample_outputs/`.
