# Demo Inputs

The inputs in `examples/demo_inputs/` are synthetic AI action proposals. Each one
is evaluated under `policy_v1.yml`, then replayed under `policy_v2.yml`.

No file contains real customer, employee, vendor, or system data.
