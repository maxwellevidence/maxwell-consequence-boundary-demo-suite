# Sample Outputs

This folder contains prebuilt sample outputs for quick inspection.

They are generated from the synthetic inputs in `examples/demo_inputs/` and are safe to include in the public package.
