# Tamper Detection Note

The tamper lab copies an allowed run, changes a manifest-bound file, and verifies that the hash mismatch is detected. This is a local proof surface, not a production sealing system.
