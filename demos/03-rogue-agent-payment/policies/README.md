# Policies

`payment_authority_policy.yml` defines the public-safe payment-control rules for this demo.

It is intentionally simple and deterministic so reviewers can inspect the rule path and compare it to the generated decision receipts.
