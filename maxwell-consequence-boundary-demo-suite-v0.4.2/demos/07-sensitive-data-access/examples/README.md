# Demo Inputs

The files under `examples/demo_inputs/` are synthetic data-access requests.

They are designed to exercise role, purpose, dataset scope, data classification, legal basis,
data minimization, and prompt-injection boundary checks.
