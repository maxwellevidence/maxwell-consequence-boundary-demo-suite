# Artifacts

Generated demo runs are written to:

```text
artifacts/runs/
```

That directory is intentionally ignored for local generated output.

Public-safe sample outputs are included under:

```text
artifacts/sample_outputs/runs/
```

No real payment, invoice, vendor, bank, customer, or employee data is included.
