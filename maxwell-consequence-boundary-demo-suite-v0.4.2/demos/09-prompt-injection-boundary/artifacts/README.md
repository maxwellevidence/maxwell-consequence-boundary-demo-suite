# Artifacts

`artifacts/runs/` is generated by `make demo` and is intentionally excluded from clean public packages.

`artifacts/sample_outputs/` contains intentionally generated sample output for public review.
