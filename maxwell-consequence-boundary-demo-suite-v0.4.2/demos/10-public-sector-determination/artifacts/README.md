# Artifacts

`artifacts/runs/` is created when you run `make demo`.

`artifacts/sample_outputs/runs/` contains sample outputs generated from the synthetic demo inputs.

Generated run artifacts are intentionally reviewable JSON records rather than hidden binary state.
