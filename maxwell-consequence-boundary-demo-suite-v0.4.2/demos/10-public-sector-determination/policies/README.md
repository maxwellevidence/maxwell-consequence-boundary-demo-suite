# Policies

This directory contains a synthetic public-preview policy used by the demo. It is intentionally small and deterministic so reviewers can inspect why each run creates, routes, or suppresses a determination effect.

The policy is not legal guidance, not a government eligibility rule set, and not a production configuration.
