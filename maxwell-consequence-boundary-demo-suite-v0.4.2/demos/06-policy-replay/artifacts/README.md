# Artifacts

Generated artifacts are written here by `make demo` and `make replay`.

```text
artifacts/runs/      original policy-at-the-time records
artifacts/replay/    replay reports under the newer policy
```

Only `artifacts/sample_outputs/` is intended to be committed or shipped in the
public ZIP.
