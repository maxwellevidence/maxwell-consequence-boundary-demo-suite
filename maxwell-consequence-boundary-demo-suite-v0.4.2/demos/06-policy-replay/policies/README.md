# Policies

This folder contains two synthetic policies:

```text
policy_v1.yml  original policy-at-the-time
policy_v2.yml  newer policy used for replay
```

The policy files are deliberately simplified so reviewers can understand why
replay outcomes change.
